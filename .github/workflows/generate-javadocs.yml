name: Generate and Deploy Javadocs

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Check out the code from the repository
      - name: Check out the repository
        uses: actions/checkout@v2

      # Step 2: Set up Java (specify the distribution and version)
      - name: Set up JDK 11
        uses: actions/setup-java@v2
        with:
          java-version: '11'
          distribution: 'temurin'  # You can also use 'zulu', 'adopt', etc., if preferred

      # Step 3: Generate Javadocs
      - name: Generate Javadocs
        run: |
          mkdir -p javadocs
          javadoc -d javadocs -sourcepath src/main/java -subpackages com.fitnessforge

      # Step 4: Deploy Javadocs to GitHub Pages
      - name: Deploy Javadocs to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: javadocs
